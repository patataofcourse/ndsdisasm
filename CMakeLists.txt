CMAKE_MINIMUM_REQUIRED(VERSION 3.10)
ADD_SUBDIRECTORY(capstone-4.0.1)
PROJECT(ndsdisasm)
ADD_EXECUTABLE(ndsdisasm main.c disasm.c)
ADD_DEPENDENCIES(ndsdisasm capstone-static)
TARGET_INCLUDE_DIRECTORIES(ndsdisasm PRIVATE capstone-4.0.1/include)
TARGET_LINK_DIRECTORIES(ndsdisasm PRIVATE ${CMAKE_BINARY_DIR}/capstone-4.0.1)
TARGET_LINK_LIBRARIES(ndsdisasm PRIVATE capstone)
